def commandlist():
  ''' Returns list of commands
      This is manually configured so that you can enabled/disable
      command as you see fit, and we don't do an insecure glob load
      Type: (list)
  '''
  commands = [ "timezones",
               "players",
             ]
  return commands

